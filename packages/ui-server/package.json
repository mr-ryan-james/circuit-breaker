{
  "name": "@circuit-breaker/ui-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/server.ts --target=bun --outdir dist",
    "clean": "rm -rf dist",
    "test": "bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@circuit-breaker/core": "workspace:*",
    "@circuit-breaker/shared-sqlite": "workspace:*",
    "hono": "^4.9.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "bun-types": "^1.3.8",
    "typescript": "^5.9.2"
  }
}
